# BASE
python train.py

# EXTENSION
python train.py --include-cbam True

# Dataset 
Ensure that test_data.pth.tar, train_data.pth.tar, val_data.pth.tar, and ALLFIXATIONMAPS/ALLFIXATIONMAPS are all located in a folder named "data" in the same directory as train.py